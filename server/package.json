{
  "name": "@fretstop/server",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "shared",
    "api",
    "ffmpeg"
  ],
  "description": "Fret Stop Server Workspace",
  "scripts": {
     "build": "yarn workspaces run build"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
